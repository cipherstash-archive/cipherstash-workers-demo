{
  "name": "cipherstash-worker-demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "app:build": "npm --prefix app run build",
    "app:dev": "npm --prefix app run dev",
    "worker:dev": "wrangler dev",
    "postinstall": "npm --prefix app install",
    "publish:worker": "wrangler publish",
    "publish:app": "./scripts/deploy-pages.sh",
    "setup:create-source-key": "./scripts/create-source-key.sh"
  },
  "dependencies": {
    "@cipherstash/stashjs-worker": "^1.0.8",
    "@headlessui/react": "^1.7.4",
    "itty-router": "^2.6.6",
    "mime": "^3.0.0",
    "uuid": "^9.0.0",
    "zod": "^3.19.1"
  },
  "devDependencies": {
    "@cipherstash/stash-cli": "^0.5.6",
    "@cloudflare/workers-types": "^3.18.0",
    "@types/mime": "^3.0.1",
    "@types/uuid": "^8.3.4",
    "typescript": "^4.8.4",
    "wrangler": "2.2.0"
  }
}
